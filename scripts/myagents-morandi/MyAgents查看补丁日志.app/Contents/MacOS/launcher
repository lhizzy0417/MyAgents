#!/bin/zsh
set -euo pipefail

LOG_PATH="${HOME}/Desktop/MyAgents-莫兰迪绿-安装日志.txt"

if [[ ! -f "${LOG_PATH}" ]]; then
  osascript -e 'display alert "还没有日志文件" message "请先运行一次绿色补丁，再来看日志。" buttons {"好"} default button 1'
  exit 1
fi

open -a TextEdit "${LOG_PATH}"
